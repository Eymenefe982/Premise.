{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "premise",
      "runtimeExecutable": "py",
      "runtimeArgs": [
        "app.py"
      ],
      "port": 8765,
      "autoPort": true
    },
    {
      "name": "premise-sandbox",
      "runtimeExecutable": "py",
      "runtimeArgs": [
        "-c",
        "import os, sys, runpy, tempfile; os.environ.update(DATABASE_URL='', LITRAG_DB=os.path.join(tempfile.gettempdir(), 'premise-sandbox.db'), OPEN_BROWSER='0', ADMIN_EMAIL='', ADMIN_PASSWORD='', PORT=os.environ.get('PORT', '8766')); sys.argv = ['app.py']; runpy.run_path('app.py', run_name='__main__')"
      ],
      "port": 8766,
      "autoPort": true
    }
  ]
}
